<?xml version="1.0" encoding="utf-8"?>
<!-- Wisma launcher background (real app icon pass, 2026-09-13) — plain
     white, matching the white rounded card baked into the new foreground
     artwork (WISMA icon.png) so the adaptive-icon mask edge is invisible
     regardless of which shape (circle/squircle/rounded-square) a given
     launcher applies. Replaces the earlier electric-blue placeholder fill.
     Kept as a vector (no new asset needed, resolution-independent) — only
     the foreground (the actual glyph) needed a raster asset. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M0,0h108v108h-108z" />
</vector>
